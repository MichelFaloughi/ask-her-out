# Storage and email (already in use)
KV_REST_API_URL=
KV_REST_API_TOKEN=
RESEND_API_KEY=
GOOGLE_MAPS_API_KEY=

# Payments (feature flag, OFF unless set to exactly "true")
PAYMENTS_ENABLED=false
# From the Stripe dashboard or the Vercel Marketplace Stripe integration
STRIPE_SECRET_KEY=
# Signing secret of the Stripe webhook endpoint pointing at /api/stripe-webhook (event: checkout.session.completed)
STRIPE_WEBHOOK_SECRET=
